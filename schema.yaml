openapi: 3.1.0
info:
  title: ACC Cost Management Wizard GPT
  description: >
    This API specification is meant to be used with the Autodesk ACC Cost Management API. It feeds into a custom GPT meant to assist with creating custom demo environments in ACC. It will evolve as I add more endpoints.
  version: 1.0.0
servers:
  - url: https://developer.api.autodesk.com/
paths:
  /construction/admin/v1/accounts/{accountId}/projects:
    get:
      summary: Retrieve Projects for an Account specified by the user in this format "8d3b74be-3fa3-408c-a2e3-91553757c743"
      operationId: getProjectsForAccount
      parameters:
        - name: accountId
          in: path
          description: The Autodesk account ID.
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/templates:
    get:
      summary: Retrieves a list of budget code templates for the specified container. Templates include IDs and names used to structure cost data.
      operationId: getBudgetCodeTemplates
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/templates/{templateId}/segments:
    get:
      summary: Retrieves a list of budget code segments for the specified template. Segments include IDs and names used to structure cost data.
      operationId: getBudgetCodeSegments
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: templateId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      summary: Creates a new segment in the budget code template.
      operationId: createBudgetCodeSegment
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: templateId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  maxLength: 1024
                  description: The name of the budget code template.
                type:
                  type: string
                  enum: [code, column, info]
                  default: code
                  description: The segment type.
                delimiter:
                  type: string
                  enum: [none, space, point, hyphen, underscore, tab]
                  default: none
                  description: The delimiter that follows the segment.
                length:
                  type: number
                  default: 100
                  description: The number of characters allowed in the segment.
                isVariableLength:
                  type: boolean
                  default: false
                  description: Whether the segment is flexible length.
                position:
                  type: number
                  description: The order of the segment in the budget code template.
                sampleCode:
                  type: string
                  description: A code sample for the segment used to demonstrate how the segment looks when displayed.
      responses:
        "201":
          description: Created
  /cost/v1/containers/{containerId}/segment-values:
    get:
      summary: Retrieves a list of budget code segment values for the specified container.
      operationId: getBudgetCodeSegmentValues
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/budgets:
    get:
      summary: Retrieves a list of budgets for the specified container.
      operationId: getBudgets
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [subitems, attributes, contract, mainContract, mainContractItem, segments, idOnly, compounded]
          description: Nested resources to include in the response.
        - name: filter[rootId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Filter by root item IDs (comma-separated).
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Filter by item IDs (comma-separated).
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Filter by last modified date (ISO 8601).
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: Filter by external ERP system name.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Filter by external ERP system IDs.
        - name: filter[code]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Filter by codes (comma-separated, quoted if numeric).
        - name: offset
          in: query
          schema:
            type: integer
          description: Records to skip for pagination.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: Max records per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort order, e.g., sort=name desc.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a new budget line item for the specified container.
      operationId: createBudget
      parameters: 
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
                  description: Unique code compliant with the budget code template defined by the project admin. Ignored if segmentCodeMap is defined.
                  maxLength: 255
                segmentCodeMap:
                  type: object
                  description: Map of budget code segments to be used in the budget code, required when updating root budget code with variable length segment. Key is the budget code segment ID, value is the code for the segment.
                  additionalProperties:
                    type: string
                    maxLength: 255
                name:
                  type: string
                  description: Name of the budget.
                  maxLength: 1024
                description:
                  type: string
                  description: Detailed description of the budget.
                quantity:
                  type: number
                  description: The quantity of labor, material, and other items planned for the budget.
                inputQuantity:
                  type: number
                  nullable: true
                  description: The input quantity planned for the budget.
                unitPrice:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: Unit price of a budget.
                unit:
                  type: string
                  description: Unit of measures used in the budget.
                actualQuantity:
                  type: number
                  description: Actual quantity of labor, material,… planned for a budget.
                actualUnitPrice:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: Actual unit price of a budget.
                actualCost:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The total amount of actual cost of the budget.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/budgets/{budgetId}:
    patch: 
      summary: Updates an existing budget line item for the specified container.
      operationId: updateBudget 
      parameters: 
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: budgetId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                parentId:
                  type: string
                  nullable: true
                  description: ID of the parent budget, used only when creating sub budgets.
                code:
                  type: string
                  description: Unique code compliant with the budget code template defined by the project admin. Ignored if segmentCodeMap is defined.
                  maxLength: 255
                scope:
                  type: string
                  enum: [budgetOnly, budgetAndCost]
                  description: The scope of the budget.
                segmentCodeMap:
                  type: object
                  description: Map of budget code segments to be used in the budget code, required when updating root budget code with variable length segment. Key is the budget code segment ID, value is the code for the segment.
                  additionalProperties:
                    type: string
                    maxLength: 255
                name:
                  type: string
                  description: Name of the budget.
                  maxLength: 1024
                quantity:
                  type: number
                  description: The quantity of labor, material, and other items planned for the budget.
                inputQuantity:
                  type: number
                  nullable: true
                  description: The input quantity planned for the budget.
                description:
                  type: string
                  description: Detail description of the budget.
                unitPrice:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: Unit price of a budget.
                unit:
                  type: string
                  description: Unit of measures used in the budget.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/budgets%3Aimport:
    post:
      summary: Imports a list of budgets into a project.
      operationId: importBudgets
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: force
          in: query
          schema:
            type: boolean
          description: Forces an override of locked budgeting.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - data
              properties:
                data:
                  type: array
                  items:
                    type: object
                  description: A list of budgets to import.
                append:
                  type: boolean
                  description: True to append, false to purge existing.
      responses:
        "204":
          description: Success
  /cost/v1/containers/{containerId}/contracts:
    get:
      summary: Retrieves a list of contracts for the specified container.
      operationId: getContracts
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results. Used together with limit to paginate through results, where offset specifies the starting point and limit specifies the number of records to return.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page. Default 100. A page may contain fewer records than the limit if there are fewer matching items or if it is the last page of results.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results. Each attribute can be sorted in asc (default) or desc order. For example, sort=name desc sorts the results by name in descending order.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs. Separate multiple IDs with commas. For example, filter[id]=id1,id2.
        - name: filter[source]
          in: query
          schema:
            type: string
            maxLength: 255
          description: Returns only items with the specified source. For example, filter[source]=urn:adsk.wipqa:bc.package:dekqaSc4SRK7AJ9YLVLCeg.
        - name: filter[code]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only items that are identified by the specified codes (separated by commas). For example, filter[code]="2703,2704". Note that even single codes in number format must be enclosed in double quotes.
        - name: filter[status]
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [draft, pending, submitted, revise, sent, signed, executed, closed, inReview]
          description: Returns only contracts with the specified statuses. Separate multiple values with commas. For example, filter[status]=draft,pending.
        - name: filter[type]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only contracts that are identified by the provided list of types. Separate multiple types with commas; for example filter[type]=type1,type2.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system. Use this name to identify or search within the integrated system. For example, filter[externalSystem]=Sage300.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system. Use this ID to track or look up data in an integrated ERP system. For example, filter[externalId]=id1,id2.
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [budgets, attributes, idOnly, scheduleOfValues, compounded]
          description: A list of resources related to the contract to include in the response. For example, include=budgets returns budgets related to the contract. include=attributes will return custom attributes which represents the "properties" in the response. If idOnly is set, the response contains only the contract IDs (id), and the other include items are ignored.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time, in ISO 8601 format. For example, filter[lastModifiedSince]=2020-03-01T13:00:00Z.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a new contract for the specified container.
      operationId: createContract
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                code:
                  type: string
                  maxLength: 255
                  description: The code of the contract.
                name:
                  type: string
                  maxLength: 1024
                  description: The name of the contract.
                description:
                  type: string
                  maxLength: 2048
                  description: The detailed description of a contract.
                companyId:
                  type: string
                  nullable: true
                  description: The ID of a company managed by a BIM 360 Admin.
                companyUid:
                  type: string
                  nullable: true
                  description: The unique ID (UUID) of the company in this account.
                type:
                  type: string
                  nullable: true
                  description: The type of contract.
                contactId:
                  type: string
                  nullable: true
                  description: The unique identifier of the default contact for the supplier.
                source:
                  type: string
                  nullable: true
                  description: The URN of the source entity from which the contract is created.
                signedBy:
                  type: string
                  nullable: true
                  description: The user who signed the contract.
                ownerId:
                  type: string
                  nullable: true
                  description: The user responsible for the purchase.
                mainContractId:
                  type: string
                  nullable: true
                  description: The ID of the main contract with which this item is associated.
                completedWorkRetentionPercent:
                  type: number
                  nullable: true
                  description: The completed work retention percentage of the contract amount.
                materialsRetentionPercent:
                  type: number
                  nullable: true
                  description: The materials retention percentage of the contract amount.
                retentionCap:
                  type: number
                  nullable: true
                  description: The maximum percentage of the total contract amount which can be used as the retention amount.
                status:
                  type: string
                  nullable: true
                  enum: [draft, pending, submitted, revise, sent, signed, executed, closed, inReview]
                  description: The status of this contract.
                subStatus:
                  type: string
                  nullable: true
                  enum: [pending, submitted, revise]
                  description: The subStatus is used when the contract is in an executed state.
                currency:
                  type: string
                  description: The code of the currency specified for the contract.
                exchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The final exchange rate for the specified currency.
                forecastExchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The forecast exchange rate.
                forecastExchangeRateUpdatedAt:
                  type: string
                  format: date-time
                  description: The last time that the forecast exchange rate was updated.
                externalId:
                  type: string
                  maxLength: 255
                  description: The identifier assigned to an item in its original external ERP system.
                externalSystem:
                  type: string
                  maxLength: 255
                  description: The name of the external ERP system integrated with Cost Management.
                externalMessage:
                  type: string
                  maxLength: 255
                  description: A message generated by the external ERP system.
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
  /cost/v1/containers/{containerId}/contracts/{contractId}:
    patch:
      summary: Updates an existing contract for the specified container.
      operationId: updateContract
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: contractId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  maxLength: 255
                  description: The code of the contract.
                name:
                  type: string
                  maxLength: 1024
                  description: The name of the contract.
                description:
                  type: string
                  maxLength: 2048
                  description: The detailed description of a contract.
                companyId:
                  type: string
                  nullable: true
                  description: The ID of a company managed by a BIM 360 Admin.
                companyUid:
                  type: string
                  nullable: true
                  description: The unique ID (UUID) of the company in this account.
                type:
                  type: string
                  nullable: true
                  description: The type of contract.
                contactId:
                  type: string
                  nullable: true
                  description: The unique identifier of the default contact for the supplier.
                source:
                  type: string
                  nullable: true
                  description: The URN of the source entity from which the contract is created.
                signedBy:
                  type: string
                  nullable: true
                  description: The user who signed the contract.
                ownerId:
                  type: string
                  nullable: true
                  description: The user responsible for the purchase.
                mainContractId:
                  type: string
                  nullable: true
                  description: The ID of the main contract with which this item is associated.
                completedWorkRetentionPercent:
                  type: number
                  nullable: true
                  description: The completed work retention percentage of the contract amount.
                materialsRetentionPercent:
                  type: number
                  nullable: true
                  description: The materials retention percentage of the contract amount.
                retentionCap:
                  type: number
                  nullable: true
                  description: The maximum percentage of the total contract amount which can be used as the retention amount.
                status:
                  type: string
                  nullable: true
                  enum: [draft, pending, submitted, revise, sent, signed, executed, closed, inReview]
                  description: The status of this contract.
                subStatus:
                  type: string
                  nullable: true
                  enum: [pending, submitted, revise]
                  description: The subStatus is used when the contract is in an executed state.
                currency:
                  type: string
                  description: The code of the currency specified for the contract.
                exchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The final exchange rate for the specified currency.
                forecastExchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The forecast exchange rate.
                forecastExchangeRateUpdatedAt:
                  type: string
                  format: date-time
                  description: The last time that the forecast exchange rate was updated.
                externalId:
                  type: string
                  maxLength: 255
                  description: The identifier assigned to an item in its original external ERP system.
                externalSystem:
                  type: string
                  maxLength: 255
                  description: The name of the external ERP system integrated with Cost Management.
                externalMessage:
                  type: string
                  maxLength: 255
                  description: A message generated by the external ERP system.
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/budgets-contracts%3Alink:
    post:
      summary: Links a budget to a contract.
      operationId: linkBudgetToContract
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                create:
                  type: array
                  items:
                    type: object
                    properties:
                      contractId:
                        type: string
                        format: uuid
                        description: ID of the Contract to which the budget to link.
                      budgetId:
                        type: string
                        format: uuid
                        description: ID of the Budget to which the contract should be linked.
                remove:
                  type: array
                  items:
                    type: object
                    properties:
                      contractId:
                        type: string
                        format: uuid
                        description: ID of the Contract to which the budget to link.
                      budgetId:
                        type: string
                        format: uuid
                        description: ID of the Budget to which the contract to link.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/schedule-of-values:
    post:
      summary: Creates a new schedule of values item for the specified container.
      operationId: createScheduleOfValuesEntry
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - name
              properties:
                id:
                  type: string
                  format: uuid
                  description: The unique ID of this SOV item.
                parentId:
                  type: string
                  nullable: true
                  description: The ID of the SOV item's parent item. For root SOV items, this value is null. Root items can only be created when allocating a budget to a contract.
                contractId:
                  type: string
                  description: The ID of the contract to which the SOV item belongs.
                budgetId:
                  type: string
                  nullable: true
                  description: The ID of the budget to which the SOV item belongs.
                code:
                  type: string
                  description: The code of the SOV item.
                  maxLength: 255
                name:
                  type: string
                  description: The name of the SOV item.
                  maxLength: 1024
                quantity:
                  type: number
                  description: The quantity of the SOV item.
                unitPrice:
                  type: number
                  description: The unit price of the SOV item.
                unit:
                  type: string
                  description: The unit of measure of the SOV item.
                  maxLength: 1024
                amount:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The total price of the SOV item.
                quantityPerBulk:
                  type: number
                  description: The quantity conversion ratio of the SOV item.
                bulkUnitPrice:
                  type: number
                  description: The unit price of the converted SOV item quantity.
                bulk:
                  type: number
                  description: The converted quantity of the SOV item.
                exchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The exchange rate that applies to the SOV item's base currency price. For example, provide the value 0.7455 for a foreign currency that's worth 0.7455 of your base currency.
                position:
                  type: number
                  description: The position of the SOV item in the list of SOV items.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/change-orders/{changeOrder}:
    get:
      summary: Retrieves a list of change orders of a specified change order type.
      operationId: getChangeOrders
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: changeOrder
          in: path
          required: true
          schema:
            type: string
            enum: [pco, rfq, rco, oco, sco]
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs.
        - name: filter[number]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that are identified by the provided list of auto-generated sequence numbers.
        - name: filter[sourceId]
          in: query
          schema:
            type: string
            format: uuid
          description: Returns only change orders that were generated from the PCO with the provided ID.
        - name: filter[contractId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only items associated with the contracts identified on this list of IDs.
        - name: filter[mainContractId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: The Main Contract ID.
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [costItems, "costItems[changeOrders]", attributes, comments]
          description: Include nested resources in the response.
        - name: filter[budgetStatus]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that are linked to budgets that have the specified status codes.
        - name: filter[costStatus]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that have the specified status codes.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a new change order for the specified container.
      operationId: createChangeOrder
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: changeOrder
          in: path
          required: true
          schema:
            type: string
            enum: [pco, rfq, rco, oco, sco]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name of the change order
                  maxLength: 1024
                description:
                  type: string
                  description: Detailed description of the change order.
                  maxLength: 2048
                scope:
                  type: string
                  enum: [out, in, tbd, budgetOnly, contingency]
                  description: The scope of the change order. Possible values are out, in, tbd, budgetOnly, contingency.
                scopeOfWork:
                  type: string
                  description: Scope of work of the change order. This is a Tiptap formatted rich text (https://tiptap.dev/introduction/).
                note:
                  type: string
                  description: Additional notes to the change order. This is a Tiptap formatted rich text (https://tiptap.dev/introduction/).
                sourceType:
                  type: string
                  nullable: true
                  description: The type of entity from which the change order is created (e.g., RFI, Issue).
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/change-orders/{changeOrder}/{id}:
    patch:
      summary: Updates the specified change order.
      operationId: updateChangeOrder
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: changeOrder
          in: path
          required: true
          schema:
            type: string
            enum: [pco, rfq, rco, oco, sco]
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 1024
                  description: The name of the change order.
                description:
                  type: string
                  maxLength: 2048
                  description: The description of the change order.
                type:
                  type: string
                  nullable: true
                  description: The type of the change order.
                scope:
                  type: string
                  enum: [out, in, tbd, budgetOnly, contingency]
                  description: The scope of the change order.
                scheduleChange:
                  type: number
                  nullable: true
                  description: The schedule change of the change order.
                proposedRevisedCompletionDate:
                  type: string
                  nullable: true
                  description: The proposed revised completion date of the change order.
                ownerId:
                  type: string
                  nullable: true
                  description: The ID of the change order’s owner/purchaser.
                scopeOfWork:
                  type: string
                  description: The scope of work of the change order.
                note:
                  type: string
                  description: Additional notes to the change order.
                exchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: A new currency exchange rate for the change order.
                companyId:
                  type: string
                  nullable: true
                  description: The BIM360/ACC ID of the company.
                companyUid:
                  type: string
                  nullable: true
                  description: The unique ID (UUID) of the company in this account.
                architectCompanyId:
                  type: string
                  nullable: true
                  description: The BIM360/ACC ID of the architecture firm.
                architectCompanyUid:
                  type: string
                  nullable: true
                  description: The unique ID (UUID) of the company in this account.
                architectContactId:
                  type: string
                  nullable: true
                  description: The BIM360/ACC ID of the primary contact at the architecture firm.
                additionalCollaborators:
                  type: array
                  items:
                    type: object
                  description: The additional collaborator company and contacts.
                sourceType:
                  type: string
                  nullable: true
                  description: The type of entity from which the change order is created.
                externalId:
                  type: string
                  maxLength: 255
                  description: The identifier assigned to an item in its original external ERP system.
                externalSystem:
                  type: string
                  maxLength: 255
                  description: The name of the external ERP system integrated with Cost Management.
                externalMessage:
                  type: string
                  maxLength: 255
                  description: A message generated by the external ERP system.
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/cost-items:
    get:
      summary: Retrieves a list of all cost items in the specified cost container for a project.
      operationId: getCostItems
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs. Separate multiple IDs with commas.
        - name: filter[number]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that are identified by the provided list of auto-generated sequence numbers.
        - name: filter[changeOrderId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: The change order ID. Separate multiple IDs with commas.
        - name: filter[budgetId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only tracking item instances that belong to the budgets identified by the provided list of budget IDs.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time, in ISO 8601 format.
        - name: filter[contractId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only items associated with the contracts identified on this list of IDs.
        - name: filter[budgetStatus]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that are linked to budgets that have the specified status codes.
        - name: filter[costStatus]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that have the specified status codes.
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page. Default 100.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results.
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [budget, changeOrders, subCostItems, attributes]
          description: A list of resources related to the returned cost items to include in the response.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a new cost item in the specified project.
      operationId: createCostItem
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: The name of the cost item to be created.
                  maxLength: 1024
                changeOrderId:
                  type: string
                  nullable: true
                  description: The ID of the change order in which the cost item is created.
                budgetId:
                  type: string
                  nullable: true
                  description: The ID of the budget to which the cost item will be linked.
                contractId:
                  type: string
                  nullable: true
                  description: The ID of the contract to which the cost item will be linked.
                description:
                  type: string
                  description: A detailed description of the cost item.
                  maxLength: 2048
                estimated:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The estimated value of the cost item provided by the general contractor (GC).
                proposed:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The quoted value of the cost item provided by the supplier in response to a Request For Quotation (RFQ).
                submitted:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The value submitted to the owner for review as part of a Change Order Request (COR).
                approved:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The value approved by the owner and recorded in the Owner Change Order (OCO).
                committed:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The value committed to the supplier after the Subcontractor Change Order (SCO) is signed.
                inputQuantity:
                  type: number
                  nullable: true
                  description: The initial quantity of the cost item as specified in the budget or contract.
                quantity:
                  type: number
                  description: The total quantity of the cost item to be used.
                unit:
                  type: string
                  description: The unit of measurement for the cost item.
                proposedExchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The exchange rate for the proposed cost item.
                committedExchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The exchange rate for the committed cost item.
                locations:
                  type: array
                  nullable: true
                  items:
                    type: string
                  description: A list of the IDs of the project locations where this item applies.
                externalId:
                  type: string
                  description: The identifier assigned to an item in its original external ERP system.
                  maxLength: 255
                externalSystem:
                  type: string
                  description: The name of the external ERP system integrated with Cost Management.
                  maxLength: 255
                externalMessage:
                  type: string
                  description: A message generated by the external ERP system that explains the sync status of the integration.
                  maxLength: 255
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/expenses:
    get:
      summary: Retrieves the requested set of expenses in the specified project.
      operationId: getExpenses
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [expenseItems, mainContract, attributes, externalRelationship, paymentReferences]
          description: A list of the nested expense resources to include in the response.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs.
        - name: filter[number]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only the items that are identified by the provided list of auto-generated sequence numbers.
        - name: filter[status]
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [draft, pending, submitted, revise, sent, signed, executed, closed, inReview]
          description: Returns only contracts with the specified statuses.
        - name: filter[mainContractId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: The Main Contract ID.
        - name: filter[budgetPaymentId]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the payments associated with the budget payments.
        - name: filter[createdAt]
          in: query
          schema:
            type: string
          description: Filter data by its create date.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time.
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system.
      responses:
        "200":
          description: OK
    post:
      summary: Creates an expense in the given project.
      operationId: createExpense
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                id:
                  type: string
                  format: uuid
                  description: The unique identifier of the expense.
                supplierId:
                  type: string
                  nullable: true
                  description: The supplier for the expense. This is the ID of a company managed by BIM 360 Admin.
                supplierCompanyUid:
                  type: string
                  nullable: true
                  description: The unique ID (UUID) of the company in this account.
                supplierName:
                  type: string
                  nullable: true
                  description: The supplier company name for the expense. Required if supplierId is not set.
                  maxLength: 255
                contractId:
                  type: string
                  nullable: true
                  description: The ID of the contract used to create expense items.
                number:
                  type: string
                  description: The auto-generated sequence number for the expense.
                  maxLength: 255
                name:
                  type: string
                  description: The name of the expense.
                  maxLength: 1024
                description:
                  type: string
                  description: The detailed description of the expense.
                  maxLength: 2048
                note:
                  type: string
                  description: The note attached to the expense.
                term:
                  type: string
                  description: The term of the expense.
                referenceNumber:
                  type: string
                  description: The user-provided reference number of the expense.
                type:
                  type: string
                  nullable: true
                  description: The type of the expense.
                  maxLength: 128
                scope:
                  type: string
                  nullable: true
                  enum: [full, partial]
                  description: The applicable scope of the expense.
                purchasedBy:
                  type: string
                  nullable: true
                  description: The user who purchased the expense.
                status:
                  type: string
                  nullable: true
                  enum: [draft, pending, revise, rejected, approved, paid]
                  description: The status of the expense.
                paymentDue:
                  type: string
                  nullable: true
                  description: The payment due date of the expense.
                issuedAt:
                  type: string
                  nullable: true
                  description: The date and time when the expense was issued.
                receivedAt:
                  type: string
                  nullable: true
                  description: The date and time when the expense was received.
                externalId:
                  type: string
                  description: The identifier assigned to an item in its original external ERP system.
                  maxLength: 255
                externalSystem:
                  type: string
                  description: The name of the external ERP system integrated with Cost Management.
                  maxLength: 255
                externalMessage:
                  type: string
                  description: A message generated by the external ERP system.
                  maxLength: 255
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/expenses/{expenseId}/items:
    get:
      summary: Retrieves the expense items and subitems of the specified expenses for a given project.
      operationId: getExpenseItems
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: expenseId
          in: path
          required: true
          schema:
            type: string
          description: A list of the object IDs of the expenses whose items you want to retrieve, separated by commas.
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [budget, contract, attributes, externalRelationship]
          description: A list of the nested expense resources to include in the response.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time.
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system.
      responses:
        "200":
          description: OK
    post:
      summary: Creates an expense item in the specified expense of a given project.
      operationId: createExpenseItem
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: expenseId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                id:
                  type: string
                  format: uuid
                  description: The unique identifier of the expense item.
                budgetId:
                  type: string
                  format: uuid
                  description: The ID of the budget to which the expense item belongs.
                budgetCode:
                  type: string
                  description: The code of an existing budget.
                contractId:
                  type: string
                  nullable: true
                  description: The ID of the contract to which the expense item belongs.
                number:
                  type: string
                  description: The sequence number for the expense item.
                  maxLength: 255
                name:
                  type: string
                  description: The name of the expense item.
                  maxLength: 1024
                description:
                  type: string
                  description: A detailed description of the expense item.
                  maxLength: 2048
                note:
                  type: string
                  description: A note to attach to the expense item.
                scope:
                  type: string
                  nullable: true
                  enum: [full, partial]
                  description: The applicable scope of the expense item.
                quantity:
                  type: number
                  description: The number of units of the expense item.
                unitPrice:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The price per unit of the expense item.
                unit:
                  type: string
                  description: The expense item’s unit of measure.
                amount:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The total price of the expense item.
                exchangeRate:
                  oneOf:
                    - type: number
                    - type: string
                    - type: "null"
                  description: The exchange rate that applies to the expense item’s base currency price.
                externalId:
                  type: string
                  description: The identifier assigned to an item in its original external ERP system.
                  maxLength: 255
                externalSystem:
                  type: string
                  description: The name of the external ERP system integrated with Cost Management.
                  maxLength: 255
                externalMessage:
                  type: string
                  description: A message generated by the external ERP system.
                  maxLength: 255
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  nullable: true
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "200":
          description: OK
  /cost/v1/containers/{containerId}/main-contracts:
    get:
      summary: Retrieves one or more of the main contracts in the given project.
      operationId: getMainContracts
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [mainContractItems, attributes]
          description: A list of the types of sub-main contract components to include in the response.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs.
        - name: filter[code]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Returns only items that are identified by the specified codes.
        - name: filter[status]
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [draft, pending, submitted, revise, sent, signed, executed, closed, inReview]
          description: Returns only main contracts that have a status on the provided list of statuses.
        - name: filter[lastModifiedSince]
          in: query
          schema:
            type: string
            format: date-time
          description: Returns only items that were modified since the specified date and time.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: The name of the external ERP system.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: The ID of the item in the external ERP system.
        - name: offset
          in: query
          schema:
            type: integer
          description: The number of records to skip before returning results.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: The maximum number of records returned per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Defines the sort order for the results.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a main contract in the given project.
      operationId: createMainContract
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - name
              properties:
                code:
                  type: string
                  maxLength: 255
                  description: The code of the main contract.
                name:
                  type: string
                  maxLength: 1024
                  description: The name of the main contract.
                description:
                  type: string
                  maxLength: 2048
                  description: Detailed description of a main contract.
                note:
                  type: string
                  description: A note about the main contract in Tiptap-formatted rich text.
                scopeOfWork:
                  type: string
                  description: The scope of work signed by all parties to the main contract, in Tiptap-formatted rich text.
                type:
                  type: string
                  description: The type of the main contract.
                contactId:
                  type: string
                  description: The BIM360/ACC ID of the supplier’s default contact.
                ownerCompanyId:
                  type: string
                  description: The BIM360/ACC ID of the owner company.
                ownerContactId:
                  type: string
                  description: The BIM360/ACC ID of the primary contact at the owner company.
                architectCompanyId:
                  type: string
                  description: The BIM360/ACC ID of the architecture firm.
                architectContactId:
                  type: string
                  description: The BIM360/ACC ID of the primary contact at the architecture firm.
                ownerCompanyUid:
                  type: string
                  description: The unique ID (UUID) of the company in this account.
                contractorCompanyUid:
                  type: string
                  description: The unique ID (UUID) of the company in this account.
                architectCompanyUid:
                  type: string
                  description: The unique ID (UUID) of the company in this account.
                notaryCompanyUid:
                  type: string
                  description: The unique ID (UUID) of the company in this account.
                startDate:
                  type: string
                  description: The start date of the main contract.
                executedDate:
                  type: string
                  description: The execution date of the main contract.
                plannedCompletionDate:
                  type: string
                  description: The planned completion date of the main contract.
                revisedCompletionDate:
                  type: string
                  description: The revised completion date of the main contract.
                actualCompletionDate:
                  type: string
                  description: The actual completion date of the main contract.
                closeDate:
                  type: string
                  description: The closing date of the main contract.
                status:
                  type: string
                  enum: [closed, executed, review, signed]
                  description: The status of the main contract.
                isDefault:
                  type: boolean
                  description: True if this is the default main contract.
                completedWorkRetentionPercent:
                  type: number
                  description: The completed work retention percentage of the main contract amount.
                materialsRetentionPercent:
                  type: number
                  description: The materials retention percentage of the main contract amount.
                retentionCap:
                  type: number
                  description: The total retention percentage of the main contract amount.
                externalId:
                  type: string
                  maxLength: 255
                  description: The identifier assigned to an item in its original external ERP system.
                externalSystem:
                  type: string
                  maxLength: 255
                  description: The name of the external ERP system integrated with Cost Management.
                externalMessage:
                  type: string
                  maxLength: 255
                  description: A message generated by the external ERP system.
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "201":
          description: Created
  /cost/v1/containers/{containerId}/main-contracts/{mainContractId}/items:
    get:
      summary: Retrieves one or more items in the specified main contracts.
      operationId: getMainContractItems
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: mainContractId
          in: path
          required: true
          schema:
            type: string
          description: The ID of the main contract.
        - name: filter[id]
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Returns only the items that are identified by the provided list of item IDs.
        - name: include
          in: query
          schema:
            type: array
            items:
              type: string
              enum: [budget, mainContract]
          description: Nested resources to include in the response.
        - name: offset
          in: query
          schema:
            type: integer
          description: Records to skip for pagination.
        - name: limit
          in: query
          schema:
            type: integer
            default: 100
          description: Max records per page.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort order.
        - name: filter[externalSystem]
          in: query
          schema:
            type: string
            maxLength: 255
          description: Filter by external ERP system name.
        - name: filter[externalId]
          in: query
          schema:
            type: array
            items:
              type: string
          description: Filter by external ERP system IDs.
      responses:
        "200":
          description: OK
    post:
      summary: Creates a main contract item in the given main contract.
      operationId: createMainContractItem
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: mainContractId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - name
              properties:
                id:
                  type: string
                  description: An ID for the main contract item.
                code:
                  type: string
                  maxLength: 255
                  description: A code for the main contract item.
                name:
                  type: string
                  maxLength: 1024
                  description: A name for the main contract item.
                description:
                  type: string
                  maxLength: 2048
                  description: A description of the main contract item.
                parentId:
                  type: string
                  description: The ID of the parent item of the main contract item.
                budgetId:
                  type: string
                  description: The ID of a budget to link to the main contract item.
                unit:
                  type: string
                  maxLength: 255
                  description: The unit of measure of the main contract item.
                unitPrice:
                  type: number
                  description: The unit price of the main contract item.
                quantity:
                  type: number
                  description: The quantity of the main contract item.
                position:
                  type: integer
                  description: The position of the main contract item in its siblings.
                externalId:
                  type: string
                  maxLength: 255
                  description: The identifier assigned to an item in its original external ERP system.
                externalSystem:
                  type: string
                  maxLength: 255
                  description: The name of the external ERP system integrated with Cost Management.
                externalMessage:
                  type: string
                  maxLength: 255
                  description: A message generated by the external ERP system.
                lastSyncTime:
                  type: string
                  format: date-time
                  description: The date and time when the item was last synchronized with the external ERP system.
                integrationState:
                  type: string
                  enum: [locked, integrated, failed]
                  description: The state of the item during the integration with the external ERP system.
      responses:
        "201":
          description: Created
  /cost/v1/containers/{containerId}/segments/{segmentId}/values%3Aimport:
    post:
      summary: Imports segment value definitions for use in a specific segment of a budget code template.
      operationId: importBudgetCodeSegmentValues
      parameters:
        - name: containerId
          in: path
          required: true
          schema:
            type: string
        - name: segmentId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - data
              properties:
                data:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                        description: The ID of the code.
                      parentId:
                        type: string
                        nullable: true
                        description: The parent ID of this code if it is the sub item of another code.
                      code:
                        type: string
                        maxLength: 255
                        description: The display code.
                      originalCode:
                        type: string
                        maxLength: 255
                        description: The original value of the code before the delimiters are removed.
                      description:
                        type: string
                        maxLength: 2048
                        description: The description of the code.
                  description: A list of segment values.
                append:
                  type: boolean
                  description: true append the imported segment values to the existing values. false purge the existing segment values and replace them with the imported values.
      responses:
        "200":
          description: OK